fastapi==0.111.0
uvicorn==0.30.1
beanie==1.26.0
motor==3.4.0
pymongo==4.8.0
redis==5.0.4
pydantic==2.7.1
pydantic-settings==2.2.1
python-multipart==0.0.9
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
google-auth==2.35.0
# google.auth.transport.requests (used by src/auth/google.py) needs this.
# It used to arrive transitively via web3; web3 moved to the blockchain/
# service, so it has to be declared here explicitly.
requests==2.32.3
# httpx is also pulled in transitively by fastapi's TestClient path; pinned
# explicitly because audit_service.py depends on it directly.
httpx==0.27.0
reportlab==4.2.5
